import styled from 'styled-components/native';

const ServicesWrapper = styled.View`
  flex: 1;
  margin-top: 24px;
`;

const ScrollCard = styled.ScrollView`
  flex: 1;
  margin-top: 24px;
`;

const ItemService = styled.View`
  flex: 1;
  margin-right: 15px;
`;

const CategoryWrapper = styled.View`
  flex: 1;
  margin-top: 24px;
`;

const Row = styled.View`
  width: 100%;
  margin-top: 24px;
  border: 1px solid #9598A8;
`;

const ScrollCategory = styled.ScrollView`
  flex: 1;
  margin-top: 13px;
`;

const ItemCategory = styled.View`
  flex: 1;
  margin-bottom: 11px;
`;

const TitleWrapper = styled.View`
  margin-top: 24px;
`;

export {
  ServicesWrapper,
  ScrollCard,
  ItemService,
  CategoryWrapper,
  Row,
  ScrollCategory,
  ItemCategory,
  TitleWrapper,
};
